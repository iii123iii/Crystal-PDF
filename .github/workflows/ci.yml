name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        version: ["latest"]
    steps:
      - uses: actions/checkout@v4
      - name: Run tests
        run: echo "CI initialized for Crystal-PDF"
