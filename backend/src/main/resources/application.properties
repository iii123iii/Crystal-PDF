spring.application.name=crystalpdf-backend
server.port=8080

# Allow large PDF uploads (up to 200 MB per file, 500 MB per request)
spring.servlet.multipart.max-file-size=200MB
spring.servlet.multipart.max-request-size=500MB
